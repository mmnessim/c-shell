#include <stdio.h>
#include "utils.h"

int main(int argc, char** argv) {
    //if (argc < 2) {
    //    printf("useage c-shell <filename>\n");
    //    return 0;
    //}
//
    //char* filename = argv[1];
//
    //cat(filename);

    ls(".");
    return 0;
}
